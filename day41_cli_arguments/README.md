# Day 41 — CLI Arguments

## Goals
- Accept inputs from the command line
- Override defaults without editing code
- Combine CLI args with config files

## Run examples
python cli_basic.py --name Amandeep --repeat 3
python cli_with_config.py --timeout 2
